import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  headerTwoBtns: {
    flex: 1,
    marginRight: 15,
    width: 80,
    flexDirection: "row",
    justifyContent: 'space-around',
    alignItems: 'center'
  }
});